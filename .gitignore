# Build outputs
lobstertrap/lobstertrap.exe
lobstertrap/lobstertrap
lobstertrap/*.test

# Runtime data
sentinel/data/*.jsonl
sentinel/data/*.png
sentinel/data/calibration_report.json
!sentinel/data/demo_canonical/

# Chroma persistence — users regenerate with `lt-agents seed`
sentinel/data/chroma_seed/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg

# Virtual env
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Notes (kept locally, not for repo)
.claude/

# Hackathon early notes (already represented in DESIGN.md)
# Keeping them tracked, just ignoring autosave junk:
*.bak

# LT-Sentinel local logs
*.log
sentinel/data/*.log

# Video build artifacts — regenerated by submission/video/build_video.py.
# caption_font.ttf is a copied system font (proprietary) — never commit it.
submission/video/audio/
submission/video/slides/
submission/video/clips/
submission/video/caption_font.ttf
submission/video/lt_sentinel_demo_subtitled.mp4
submission/video/*副本*

# Day-2 re-run scratch data — canonical dataset is sentinel/data/demo_canonical/
sentinel/data/demo_day2/

# Discarded audit screenshots (shot5 — JSONL too long to be legible on screen)
submission/video_shots/shot5_audit*
