# 设计演进 + Claude 犯的设计陷阱 + 用户最终方案

---

## Claude 在这次设计讨论里犯的错(防再犯)

### 错误 1: "凑 5 项 focus area"

我第一版方案推 "多 Agent + 信任评分 + 审计报告组合", 号称覆盖 Track 1 的 3.5/5 focus area.
用户雷达响:
> "你说的这些, 似乎全部能有思路迎刃而解, 那么没有什么差异性了啊"

教训: **横向铺开 = 宽 1cm 深 1cm 的 toy demo**. 单点深度才有差异性.
任何赛道都不要追求"覆盖所有 focus area".

### 错误 2: 推情绪值方案没核验企业前提

我顺着用户提的"情绪值"方向推, 没先问企业级场景的约束.
用户立刻反问:
> "企业的需求是什么? 是需要稳定快速答案, 还是可以允许有一定的延迟? 我们从没想清楚."

教训: **任何"借鉴游戏/社区做法到企业场景"的设计, 都要先核验**:
- 延迟容忍度
- 确定性 / 可解释性要求
- 合规要求

### 错误 3: 替用户做选择

用户两次喊出:
> "你还挺会替我选嘞"
> "我本身是有思路的"

教训: 用户的方向他自己拍, 我只列选项, 不诱导.

---

## 用户最终方案(2026-05-11 突破点)

核心三件套:

### 1. 日志可视化(顺手做, 不是主菜)

**目的**: 可解释性 + demo 演示效果
**做法**: 强制每条审计日志加**时间序列字段**
**收益**: 审计链路按时间线展开, demo 可视化好做, 也戳"audit trails" focus area

### 2. Drift 校准(不单独做)

**判断**: 这件事会作为多 Agent 或情绪值方向的"副产品"自然解决, 不另起一摊.

### 3. 情绪值评分系统 + 双重 LT (主设计)

**这是真正的创新点.**

#### 行为 → 情绪值扣分映射

| LT Action | 情绪扣分 | 说明 |
|-----------|----------|------|
| DENY | 大扣 | 三个"硬拦截"之一 |
| HUMAN_REVIEW | 大扣 | 三个"硬拦截"之一 |
| QUARANTINE | 大扣 | 三个"硬拦截"之一 |
| LOG | 设计性扣分 | 中间地带, 扣多少要设计 |
| RATE_LIMIT | 设计性扣分 | 中间地带, 扣多少要设计 |
| MODIFY | 不管 | LT 标 reserved, 暂不参与 |
| REDIRECT | 不管 | LT 标 reserved, 暂不参与 |
| ALLOW | 不扣(可设计恢复) | — |

#### 双重 LT (Double LT) — 真正的差异化点

情绪值跌破阈值 → **激活第二层 LT**, 对该 Agent 的输出做更严格的检查.

这是 **adaptive security** 概念在 LLM Agent 治理上的应用:
- 行业有这个概念(网络安全)
- 但在 **LLM Agent governance** 领域**少有人做**
- 这是用户的原创组合

#### 单一机制多重效用(规避"凑 5 项"陷阱)

这一套设计同时戳到:
- **Guardrails** — 双重 LT 是动态护栏
- **Monitoring** — 情绪值是实时监控指标
- **Audit Trails** — 每次扣分都是审计事件, 形成因果链
- **Access Control** — 情绪低于阈值的 Agent 可触发权限收紧

**4/5 focus area, 但都是同一套机制衍生出来的**. 不是凑, 是自然辐射.

---

## 这套设计的可辩护性

| 维度 | LT 单层 | 用户的双重 LT |
|------|---------|---------------|
| 延迟 | <5ms | 大部分请求只过单层(延迟相同), 触发阈值后才双层(可控开销) |
| 确定性 | 规则确定 | 规则确定 + 情绪值规则化(也是 deterministic) |
| 可解释性 | 单次决策可解释 | **每次情绪变化都有事件链**, 比单层更可解释 |
| 红队对抗 | 单层规则可绕 | 多次绕规会被扣分累积, 触发二层 |

**关键**: 用户的设计**自然解决了 Claude 之前担心的可解释性问题** —— 因为每次情绪扣分都是显式事件, 审计日志自动产生因果链.

---

## 后续工作分阶段

### Phase 0: 设计冻结(未做)

需先回答 [03-unresolved-questions.md](03-unresolved-questions.md) 的 6 个问题再开工.

### Phase 1: MVP

- Fork Veea Lobster Trap
- 加情绪值状态(per-agent)
- 加扣分逻辑(action → 扣分映射)
- 加阈值触发的"启动双重 LT"开关
- 第二层 LT 规则集(待设计, 见未决问题 4)

### Phase 2: Demo 场景实装

选定具体场景(医疗/金融/法律/通用 SaaS, 见未决问题 6), 跑通端到端 demo.

### Phase 3: Presentation

- 演示视频
- 时间序列审计 dashboard
- 红队对抗演示(展示绕规会被双重 LT 抓)

---

## 时间预算约束

- **比赛截止时间**: 待查
- **用户其他线**: BOSS 投递(主线, 每天 1+1 小时) + 面试 + 简历优化
- **黑客松定位**: 当前是次要线, 设计阶段优先级低, **不挤面试准备时间**
