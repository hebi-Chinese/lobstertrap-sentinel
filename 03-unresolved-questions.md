# 未决设计问题

回来继续之前, 这 6 个问题必须先有答案. 顺序大致按优先级降序.

---

## Q1: 情绪值恢复曲线

**问题**: 情绪掉下去之后, 怎么恢复?

候选方案:
- **不恢复** — 一次扣分永久记账(最严格, 但 Agent 一旦被打入冷宫永世不得翻身)
- **时间窗口衰减** — N 分钟没有违规, 自动 +X
- **行为驱动恢复** — 正常调用累积 ALLOW 次数到一定值后回血
- **混合** — 时间衰减 + 行为加速

**需要拍**: 选哪种 + 具体参数(窗口大小 / 加速曲线)

---

## Q2: 具体扣分数值

**问题**: "大扣分"和"设计性扣分"到底是多少?

候选锚定:
- 起始情绪 = 100?
- DENY/HUMAN_REVIEW/QUARANTINE 扣多少(例: -30? -50?)
- LOG / RATE_LIMIT 扣多少(例: -5? -10?)
- 触发"双重 LT"的阈值(例: <50? <70?)

**注意**: 这些数字 demo 阶段可以拍脑袋, 但必须**可调** + **有理论依据可讲**(比如"按 LT 默认策略 priority 分布反推").

---

## Q3: 跨 Agent 联动

**问题**: Agent A 情绪低, 会不会影响 Agent B?

场景: 多 Agent 协同时, A 已经表现可疑 → B 收到 A 的输出 → B 的输入算可疑吗?

候选方案:
- **完全隔离** — 每个 Agent 独立账本(简单, 但漏掉协同攻击)
- **传染机制** — A 调 B 时, A 的情绪影响 B 当次请求评分
- **图传播** — 维护 Agent 调用图, 情绪沿图传播

**Track 1 是 "Agent Security & AI Governance"** —— 多 Agent 治理是这赛道的痛点, **传染或图传播可能是差异化的强信号**.

---

## Q4: 第二层 LT 的规则集

**问题**: 双重 LT 触发后, 第二层用什么规则?

如果第二层 = 第一层规则**重复一遍** → 完全没意义, 一样的检查跑两次结果一样.

候选差异化:
- **更低阈值的现有规则** — 比如 risk_score > 0.5 触发 DENY(第一层是 > 0.8)
- **新增更细粒度规则** — 检查更多元数据字段
- **启用更慢的 ML 推理规则** — 第一层快, 第二层慢但深
- **启用语义级检查** — 不仅看模式匹配, 看含义

**核心论点**: 第二层之所以"贵"是因为更深的检查, 平时不用 是因为性能成本; 但情绪低的 Agent **已经证明可疑**, 此时多花成本是值的.

---

## Q5: 第二层 LT 部署形态

**问题**: 第二层 LT 跑在哪儿?

- **同进程, 同 Go 服务** — 用 feature flag 切换规则集, 最简单, demo 友好
- **独立进程, sidecar 模式** — 第一层把请求转发给第二层服务再回来, 解耦但加延迟
- **同进程, 不同 goroutine 池** — 第二层走单独线程池, 中间方案

**Demo 阶段建议**: 同进程 feature flag. 工程版讨论 sidecar.

---

## Q6: Demo 场景选什么

**问题**: 比赛 demo 用什么具体业务场景?

候选 + 利弊:

| 场景 | 利 | 弊 |
|------|----|----|
| 医疗 | 数据敏感性高, 故事性强 | 合规要求多, 容易被钉细节 |
| 金融 | 业务价值直观 | 同上, 且竞品多 |
| 法律 | PII / 隐私场景天然契合 LT | 受众窄, 评委可能不熟 |
| 通用 SaaS | 受众广 | 故事性弱, 没记忆点 |
| 内部 IT Agent | 贴合 JD 那个公司的"B 端业务" | 偏小众 |

**没拍**. 倾向: 通用 SaaS + 一个具体子场景(比如客服 Agent 被诱导泄露内部数据).

---

## 元层面问题(用户尚未表态, 但迟早要回答)

### M1: 是否组队?

- 单人: 节奏完全可控, 但工作量大
- 组队: 提速, 但协同开销 + 沟通成本
- 现倾向: 先单人推到能跑的程度, 必要时再找人

### M2: 是否真的提交?

- 用户最初是为了**学习**接触这个领域
- 真提交意味着按 lablab 规则走流程(注册 / 上传 demo / 录视频 / 接受评审)
- 即使最终不交, 设计本身的学习价值已经够了
- 现倾向: 推到 demo 能跑, 看时间是否允许提交
